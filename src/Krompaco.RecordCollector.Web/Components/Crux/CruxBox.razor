@{
    if (this.RecordList == null)
    {
        return;
    }
}

@foreach (var record in this.RecordList)
{
    <CruxFormFactor CurrentRecord="record" />
}

@code {
    [Parameter]
    public List<Record>? RecordList { get; set; }
}
